<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base app theme -->
    <style name="Theme.SkbAudio" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash theme (Android 12+ SplashScreen API) -->
    <style name="Theme.SkbAudio.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#050810</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_skb_logo_foreground</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.SkbAudio</item>
    </style>
</resources>
